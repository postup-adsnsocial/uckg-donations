---
phase: 01-tenant-and-production-safety-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified: [apps/api/package.json, pnpm-lock.yaml, apps/api/src/observability/logger.config.ts, apps/api/src/observability/logger.config.spec.ts, apps/api/src/observability/safe-exception.filter.ts, apps/api/src/observability/metrics.service.ts, apps/api/src/observability/metrics.service.spec.ts, apps/api/src/observability/metrics.interceptor.ts, apps/api/src/observability/observability.controller.ts, apps/api/src/observability/observability.module.ts, apps/api/src/health/health.controller.ts, apps/api/src/health/health.controller.spec.ts, apps/api/src/app.module.ts, apps/api/src/main.ts, tests/e2e/production-safety.spec.ts]
autonomous: true
requirements: [SEC-02, SEC-03, TEN-01, TEN-02, TEN-03, SEC-01]
must_haves:
  truths: ["Every response/error has a safe correlation ID and logs contain no credential, token, PII, tenant UUID, SQL, or database URL canary.", "Liveness remains up without PostgreSQL while readiness returns a generic 503.", "Protected metrics expose only allowlisted low-cardinality traffic, latency, error, throttle, login and pool signals.", "The full real-application phase gate proves all six requirements."]
  artifacts:
    - {path: "apps/api/src/observability/logger.config.ts", provides: "JSON correlation and redaction policy"}
    - {path: "apps/api/src/observability/metrics.service.ts", provides: "Dedicated safe Prometheus registry"}
    - {path: "tests/e2e/production-safety.spec.ts", provides: "Adversarial HTTP phase gate"}
  key_links:
    - {from: "apps/api/src/main.ts", to: "apps/api/src/observability/logger.config.ts", via: "buffered Nest Pino logger", pattern: "bufferLogs|useLogger"}
    - {from: "apps/api/src/health/health.controller.ts", to: "apps/api/src/database/database.service.ts", via: "bounded SELECT 1 readiness only", pattern: "health/ready|select 1"}
---
<objective>Deliver safe operational visibility and close the phase with adversarial application tests. Purpose: Implement D-14 through D-19 and prove the earlier boundaries together. Output: JSON logs/correlation/errors, live/ready/metrics, E2E and full gate.</objective>
<execution_context>@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md
@AGENTS.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-02-SUMMARY.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-03-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add correlation, structured allowlist logging, and safe errors</name>
<files>apps/api/package.json, pnpm-lock.yaml, apps/api/src/observability/logger.config.ts, apps/api/src/observability/logger.config.spec.ts, apps/api/src/observability/safe-exception.filter.ts</files>
<read_first>apps/api/package.json, apps/api/src/main.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/config/api-config.service.ts, .planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md</read_first>
<behavior>- Valid UUID x-correlation-id is retained; other input gets random UUID. - JSON logs contain only safe request/event fields. - Public 4xx/5xx contains correlationId but no internals.</behavior>
<action>Install exact nestjs-pino@4.6.1, pino@10.3.1, pino-http@11.0.0, prom-client@15.1.3. Build `genReqId` with strict UUID acceptance and x-correlation-id response. Allowlist only correlation ID, method, route template, status class, duration and bounded event/outcome/scope; never raw URL/query/headers/body, tenant/user/account identifiers. Add defense redaction paths for authorization, cookie, x-metrics-token, body, set-cookie, password, token, email, fullName, phone. SafeExceptionFilter emits generic status/message/correlationId and never stack/SQL/config/raw error. Canary tests inject password, session token, authorization, cookie, email, name, phone, church UUID and DB URL and scan parsed JSON byte-for-byte.</action>
<acceptance_criteria>Every captured line parses JSON with UUID correlation; all canaries are absent; filter tests prove stack/SQL/config absent from body/log; dependencies are exact versions; targeted tests exit 0.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/observability/logger.config.spec.ts</automated></verify>
<done>Operators receive correlation without sensitive logging or public error detail.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Separate live/ready and expose protected bounded metrics</name>
<files>apps/api/src/observability/metrics.service.ts, apps/api/src/observability/metrics.service.spec.ts, apps/api/src/observability/metrics.interceptor.ts, apps/api/src/observability/observability.controller.ts, apps/api/src/health/health.controller.ts, apps/api/src/health/health.controller.spec.ts</files>
<read_first>apps/api/src/health/health.controller.ts, apps/api/src/health/health.controller.spec.ts, apps/api/src/database/database.service.ts, apps/api/src/config/api-config.service.ts, apps/api/src/security/login-throttler.guard.ts</read_first>
<behavior>- /health and /health/live stay 200 without DB. - /health/ready runs bounded SELECT 1 and returns generic 503. - /internal/metrics requires constant-time token and fixed label allowlist.</behavior>
<action>Keep `/health` compatibility alias and add `/health/live` static `{status:'ok'}` without dependency/version/config. Add `/health/ready` with one-second bounded SELECT 1 and generic 503. Use a dedicated prom-client Registry and prefix `uckg_api_`; expose exactly http_requests_total{method,route,status_class}, http_request_duration_seconds{method,route,status_class}, login_attempts_total{outcome}, throttle_rejections_total{scope}, db_pool_connections{state=total|idle|waiting}, plus safe default metrics. Protect `/internal/metrics` with production-required METRICS_TOKEN using length-safe timing comparison and registry content type; no tenant/correlation/user/email/raw URL/error labels.</action>
<acceptance_criteria>Metric tests compare exact name/label allowlist and find no canaries; health tests simulate DB failure and assert live 200/ready generic 503; absent/wrong metrics token is rejected and correct token returns registry content type; targeted tests exit 0.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/observability apps/api/src/health</automated></verify>
<done>Health and metrics give useful bounded signals without cross-tenant or PII exposure.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Wire observability and run the adversarial phase gate</name>
<files>apps/api/src/observability/observability.module.ts, apps/api/src/app.module.ts, apps/api/src/main.ts, tests/e2e/production-safety.spec.ts</files>
<read_first>apps/api/src/app.module.ts, apps/api/src/main.ts, apps/api/src/observability/logger.config.ts, apps/api/src/observability/metrics.service.ts, apps/api/src/health/health.controller.ts, tests/e2e/auth-tenancy.spec.ts, AGENTS.md</read_first>
<behavior>- Real API proves headers/CORS/413/429/generic errors/correlation/live/ready/metrics. - Existing auth-tenancy and tenancy DB attacks remain green. - No rendered UI changes occur.</behavior>
<action>Register LoggerModule/ObservabilityModule, APP_INTERCEPTOR metrics, and global safe exception filter; bootstrap with bufferLogs true and app.useLogger. Create E2E that runs against runtime DATABASE_URL and asserts Helmet headers, allowlisted/rejected CORS, 413 over 256 KiB, generic throttling, cookie Secure in production fixture, correlation on success/error, no internals, live during DB outage, generic ready 503, protected metrics and safe exposition. Also exercise missing tenant, cross-tenant API, and permission denial. Do not add UI, envelope image/PDF storage, audit, jobs, deploy infrastructure, or visual baseline changes. Run visual only if execution unexpectedly changes rendered output; then follow PT-BR/en/es desktop/mobile/narrow inspection rules.</action>
<acceptance_criteria>E2E file contains assertions for 413, 429, CORS, Helmet, correlation, live, ready 503 and metrics 401/200; `pnpm test:e2e`, tenancy test and `pnpm check:full` exit 0; git diff contains no web files or visual snapshots unless explicitly justified and inspected in SUMMARY.</acceptance_criteria>
<verify><automated>pnpm exec playwright test tests/e2e/production-safety.spec.ts --project=chromium &amp;&amp; pnpm check:full</automated></verify>
<done>All six Phase 1 requirements pass through the actual runtime topology and repository quality gate.</done>
</task>
</tasks>
<verification>`pnpm test &amp;&amp; pnpm test:migrations &amp;&amp; pnpm --filter @uckg/database test:tenancy &amp;&amp; pnpm test:e2e &amp;&amp; pnpm check:full` passes.</verification>
<success_criteria>Production-safe operations are observable without sensitive data, and every tenant/security truth has automated proof.</success_criteria>
<output>After completion, create `.planning/phases/01-tenant-and-production-safety-foundation/01-04-SUMMARY.md`.</output>
