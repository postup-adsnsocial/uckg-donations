---
phase: 01-tenant-and-production-safety-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - .env.example
  - apps/api/src/config/api-config.ts
  - apps/api/src/config/api-config.spec.ts
  - apps/api/src/config/api-config.service.ts
  - packages/database/src/index.ts
  - apps/api/src/database/database.service.ts
  - apps/api/src/app.module.ts
  - apps/api/src/main.ts
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "Unsafe or incomplete production configuration prevents API listen."
    - "Runtime database, CORS, proxy, body and pool settings have validated explicit values."
  artifacts:
    - path: "apps/api/src/config/api-config.ts"
      provides: "Production-aware Zod environment contract"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "apps/api/src/config/api-config.service.ts"
      via: "validated bootstrap values"
      pattern: "ApiConfigService"
---
<objective>Build typed fail-fast configuration and hardened HTTP/database bootstrap. Purpose: implement D-09/D-11/D-13 and supply dependencies to later security/observability plans. Output: config contract, bounded pool, Helmet/CORS/body/proxy settings.</objective>
<execution_context>@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-01-SUMMARY.md
@AGENTS.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Define dependencies and production environment contract</name>
<files>apps/api/package.json, pnpm-lock.yaml, .env.example, apps/api/src/config/api-config.ts, apps/api/src/config/api-config.spec.ts, apps/api/src/config/api-config.service.ts</files>
<read_first>apps/api/package.json, package.json, .env.example, apps/api/src/main.ts, apps/api/src/database/database.service.ts, packages/database/scripts/migrate.ts</read_first>
<behavior>- production requires runtime DATABASE_URL, WEB_ORIGINS, TRUST_PROXY, METRICS_TOKEN and every limit. - localhost/wildcard/implicit proxy fail. - dev/test defaults remain.</behavior>
<action>Install exact @nestjs/config 4.0.4, @nestjs/throttler 6.5.0, helmet 8.3.0, nestjs-pino 4.6.1, pino 10.3.1, pino-http 11.0.0, prom-client 15.1.3, zod 4.0.14. Define NODE_ENV, API_PORT, DATABASE_URL (runtime only), WEB_ORIGINS, TRUST_PROXY, METRICS_TOKEN, BODY_LIMIT=256kb, DB_POOL_MAX=10, DB_CONNECTION_TIMEOUT_MS=5000, DB_IDLE_TIMEOUT_MS=30000, DB_STATEMENT_TIMEOUT_MS=15000, DB_READY_TIMEOUT_MS=1000, LOGIN_SOURCE_LIMIT=10/60000, LOGIN_ACCOUNT_LIMIT=5/900000. Production errors name keys not values and reject localhost DB/origins, wildcard CORS and trust proxy true. .env.example documents separate MIGRATION_DATABASE_URL, provisional co-located us-east-1 only as a deployment note, and no secrets.</action>
<acceptance_criteria>All exact packages/keys/defaults appear; config test covers every missing/insecure case and secret-canary absence; migrate.ts remains MIGRATION_DATABASE_URL-only; test exits 0.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/config/api-config.spec.ts</automated></verify>
<done>One validated contract owns all production settings.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Consume config in database and HTTP bootstrap</name>
<files>packages/database/src/index.ts, apps/api/src/database/database.service.ts, apps/api/src/app.module.ts, apps/api/src/main.ts</files>
<read_first>packages/database/src/index.ts, apps/api/src/database/database.service.ts, apps/api/src/app.module.ts, apps/api/src/main.ts, apps/api/src/config/api-config.service.ts, packages/database/scripts/migrate.ts</read_first>
<behavior>- API never receives migrator URL. - pool bounds are explicit. - Helmet, allowlisted credentialed CORS, explicit proxy and 256kb parsers run before routes.</behavior>
<action>Register global cached ConfigModule/ApiConfigService. Parameterize createDatabase pool max/timeouts/query limits/application_name `uckg-api`. Remove direct process.env/default reads from API database/main. Configure validated port, exact trust proxy, helmet, JSON and urlencoded 256kb, credentialed origin allowlist and shutdown hooks before listen. Do not configure logger/metrics here; Plan 06 owns those same composition files later and depends on this plan.</action>
<acceptance_criteria>No process.env occurs in main/database service; no MIGRATION_DATABASE_URL occurs in API source; pool options and HTTP protections are grep-visible; config tests and typecheck pass.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/config apps/api/src/database &amp;&amp; pnpm --filter @uckg/api typecheck</automated></verify>
<done>API starts only from validated runtime-safe configuration.</done>
</task>
</tasks>
<verification>`pnpm exec vitest run apps/api/src/config apps/api/src/database` passes.</verification>
<success_criteria>Production misconfiguration fails before listen and request/database resources are bounded.</success_criteria>
<output>After completion create `01-04-SUMMARY.md`.</output>
