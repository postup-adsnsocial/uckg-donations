---
phase: 01-tenant-and-production-safety-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: [apps/api/package.json, pnpm-lock.yaml, .env.example, apps/api/src/config/api-config.ts, apps/api/src/config/api-config.spec.ts, apps/api/src/config/api-config.service.ts, apps/api/src/app.module.ts, apps/api/src/main.ts, apps/api/src/database/database.service.ts, packages/database/src/index.ts, apps/api/src/security/login-throttler.guard.ts, apps/api/src/security/login-throttler.guard.spec.ts, apps/api/src/auth/auth.service.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.module.ts]
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths: ["Production bootstrap fails before listening when required configuration is missing, invalid, local, or insecure.", "Login is independently bounded by source and normalized-account keys without enumeration.", "Responses use secure headers, allowlisted credentialed CORS, explicit body limits, and production-secure cookies."]
  artifacts:
    - {path: "apps/api/src/config/api-config.ts", provides: "Zod production startup contract"}
    - {path: "apps/api/src/security/login-throttler.guard.ts", provides: "Source/account login limits"}
  key_links:
    - {from: "apps/api/src/main.ts", to: "apps/api/src/config/api-config.service.ts", via: "validated bootstrap values", pattern: "ApiConfigService"}
    - {from: "apps/api/src/auth/auth.controller.ts", to: "apps/api/src/security/login-throttler.guard.ts", via: "login route guard", pattern: "LoginThrottlerGuard"}
---
<objective>Fail unsafe production startup and harden the authentication/request boundary. Purpose: Implement D-09 through D-13 without frontend changes. Output: typed config, bounded pool/request policies, Helmet, CORS, cookie regression, and dual login throttling.</objective>
<execution_context>@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md
@AGENTS.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Define and test the production configuration contract</name>
<files>apps/api/package.json, pnpm-lock.yaml, .env.example, apps/api/src/config/api-config.ts, apps/api/src/config/api-config.spec.ts, apps/api/src/config/api-config.service.ts</files>
<read_first>apps/api/package.json, package.json, .env.example, apps/api/src/main.ts, apps/api/src/database/database.service.ts, packages/database/src/index.ts</read_first>
<behavior>- Production rejects absent DATABASE_URL, WEB_ORIGINS, TRUST_PROXY, METRICS_TOKEN and limits. - Production rejects localhost DB/origins and wildcard CORS. - Dev/test retains local defaults.</behavior>
<action>Install exact `@nestjs/config@4.0.4`, `@nestjs/throttler@6.5.0`, `helmet@8.3.0`, `zod@4.0.14`. Add Zod validation and typed service for NODE_ENV; API_PORT; DATABASE_URL; WEB_ORIGINS comma-list; TRUST_PROXY (`false` or explicit CIDR/hop policy, never implicit true); METRICS_TOKEN; BODY_LIMIT=`256kb`; DB_POOL_MAX=10; DB_CONNECTION_TIMEOUT_MS=5000; DB_IDLE_TIMEOUT_MS=30000; DB_STATEMENT_TIMEOUT_MS=15000; DB_READY_TIMEOUT_MS=1000; LOGIN_SOURCE_LIMIT=10/WINDOW_MS=60000; LOGIN_ACCOUNT_LIMIT=5/WINDOW_MS=900000. Defaults apply only development/test. Error messages list key names, never values. Document keys in .env.example, keeping runtime DATABASE_URL distinct from MIGRATION_DATABASE_URL and noting provisional co-located us-east-1 as deployment documentation only.</action>
<acceptance_criteria>Config spec tests every required production key plus localhost/wildcard/trust-proxy rejection and dev defaults; source contains all exact keys/default numbers; error assertions ensure canary secret absent; targeted test exits 0.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/config/api-config.spec.ts</automated></verify>
<done>Configuration is typed, complete, provider-neutral, and production fail-fast.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Consume validated config for pool and HTTP hardening</name>
<files>apps/api/src/app.module.ts, apps/api/src/main.ts, apps/api/src/database/database.service.ts, packages/database/src/index.ts</files>
<read_first>apps/api/src/app.module.ts, apps/api/src/main.ts, apps/api/src/database/database.service.ts, packages/database/src/index.ts, apps/api/src/config/api-config.service.ts, apps/api/src/auth/auth.controller.ts</read_first>
<behavior>- No production path reads fallback process.env values. - Pool uses explicit max/timeouts. - API sends Helmet headers, allowlisted credentialed CORS, 256 KiB limits.</behavior>
<action>Register ConfigModule globally with validate/cache and inject ApiConfigService. Change createDatabase to accept pool options and configure max 10, connection 5s, idle 30s, statement/query 15s, application_name `uckg-api`. Bootstrap with validated port/origins/trust proxy; install Helmet before routes; set Express json/urlencoded limits 256kb; keep credentials true and reject non-allowlisted origins. Preserve HttpOnly/SameSite=Strict and use validated production boolean for Secure. The API receives only runtime DATABASE_URL, never MIGRATION_DATABASE_URL.</action>
<acceptance_criteria>`rg 'process\.env' apps/api/src/main.ts apps/api/src/database/database.service.ts` returns no matches; createDatabase exposes all pool bounds; main contains helmet, both 256kb parsers, explicit trust proxy and origin allowlist; config/database tests pass.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/config apps/api/src/database</automated></verify>
<done>Startup and request transport consume only validated safe settings.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Add non-enumerating source and account login throttling</name>
<files>apps/api/src/security/login-throttler.guard.ts, apps/api/src/security/login-throttler.guard.spec.ts, apps/api/src/auth/auth.service.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.module.ts</files>
<read_first>apps/api/src/auth/auth.service.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.module.ts, packages/authorization/src/password.ts, packages/contracts/src/index.ts, apps/api/src/config/api-config.service.ts</read_first>
<behavior>- Source counts all attempts at 10/60s. - SHA-256 normalized-account key counts failures at 5/15m for existing and nonexistent users. - Unknown/inactive/wrong-password return identical 401 and generic 429.</behavior>
<action>Use named @nestjs/throttler policies; source key uses request.ip only after validated proxy policy, account key is SHA-256 of trimmed lowercase email and is never logged/returned. Invalid bodies still hit source limit. Unknown/inactive users execute one verification against a fixed valid dummy scrypt hash to normalize work. Return generic 401 and generic 429; keep the locked 6–128 password rule and cookie flags. Document in code/config that built-in storage supports one initial API replica; multi-replica edge/shared limiting is launch infrastructure, not a custom Phase 1 store.</action>
<acceptance_criteria>Tests assert independent keys/windows, normalization, direct/forwarded proxy behavior, unknown-account dummy verification, identical 401, generic 429, and absence of raw email/password in tracker/errors; password source/spec still assert 6 and 128 bounds; command exits 0.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/security/login-throttler.guard.spec.ts apps/api/src/auth packages/authorization/src/password.spec.ts</automated></verify>
<done>Login abuse is bounded without account enumeration or weakening the product password decision.</done>
</task>
</tasks>
<verification>`pnpm exec vitest run apps/api/src/config apps/api/src/security apps/api/src/auth` passes.</verification>
<success_criteria>Unsafe production config cannot listen, and login/HTTP behavior has conservative configurable protections.</success_criteria>
<output>After completion, create `.planning/phases/01-tenant-and-production-safety-foundation/01-03-SUMMARY.md`.</output>
