---
phase: 01-tenant-and-production-safety-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [apps/api/src/database/tenant-unit-of-work.ts, apps/api/src/database/tenant-unit-of-work.spec.ts, apps/api/src/database/database.module.ts, apps/api/src/auth/auth.types.ts, apps/api/src/members/members.service.ts, apps/api/src/members/members.controller.ts, apps/api/src/tenancy/route-policy.decorator.ts, apps/api/src/tenancy/route-policy.guard.ts, apps/api/src/tenancy/domain-route.decorator.ts, apps/api/src/tenancy/route-policy-inventory.spec.ts, apps/api/src/tenancy/permissions.guard.ts, apps/api/src/tenancy/permissions.guard.spec.ts, apps/api/src/tenancy/tenancy.module.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/churches/churches.controller.ts]
autonomous: true
requirements: [TEN-01, TEN-03]
must_haves:
  truths: ["Domain work cannot run without explicit church, actor, and correlation context.", "Every HTTP handler is classified and every domain handler declares a known permission.", "Missing permission metadata denies rather than allows."]
  artifacts:
    - {path: "apps/api/src/database/tenant-unit-of-work.ts", provides: "Transaction-local tenant execution boundary"}
    - {path: "apps/api/src/tenancy/domain-route.decorator.ts", provides: "Ordered auth, tenant and permission abstraction"}
    - {path: "apps/api/src/tenancy/route-policy-inventory.spec.ts", provides: "Fail-closed endpoint inventory"}
  key_links:
    - {from: "apps/api/src/members/members.service.ts", to: "apps/api/src/database/tenant-unit-of-work.ts", via: "run(context, tx callback)", pattern: "tenantUnitOfWork.*run"}
    - {from: "apps/api/src/tenancy/domain-route.decorator.ts", to: "apps/api/src/tenancy/permissions.guard.ts", via: "UseGuards in locked order", pattern: "SessionAuthGuard.*TenantGuard.*PermissionsGuard"}
---
<objective>Make explicit tenant transactions and fail-closed route policy the only domain path. Purpose: Implement D-01/D-02/D-05 through D-08 without completing members. Output: Tenant UoW, composed decorators, denial guard and inventory tests.</objective>
<execution_context>@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md
@AGENTS.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Introduce TenantContext and transactional unit of work</name>
<files>apps/api/src/database/tenant-unit-of-work.ts, apps/api/src/database/tenant-unit-of-work.spec.ts, apps/api/src/database/database.module.ts, apps/api/src/auth/auth.types.ts, apps/api/src/members/members.service.ts, apps/api/src/members/members.controller.ts</files>
<read_first>apps/api/src/database/database.service.ts, apps/api/src/database/database.module.ts, apps/api/src/auth/auth.types.ts, apps/api/src/members/members.service.ts, apps/api/src/members/members.controller.ts, packages/database/src/index.ts</read_first>
<behavior>- Context requires readonly churchId, actorId, correlationId. - run sets all three GUCs locally before invoking work. - member queries retain explicit church predicate while using only callback tx.</behavior>
<action>Record the dirty diffs for both members files first. Define immutable domain `TenantContext` and `TenantTransaction`; implement `TenantUnitOfWork.run&lt;T&gt;(context, work)` using one Drizzle transaction and parameterized `set_config('app.current_church_id'|'app.current_actor_id'|'app.correlation_id', value, true)`. Export provider from DatabaseModule. Adapt only existing member list/create plumbing to accept the explicit context and callback tx while retaining `where(eq(members.churchId, context.churchId))` and inserted `churchId`; do not add pagination, update, lifecycle, duplicate improvements, UI, or other Phase 3 behavior. Preserve all existing dirty contracts and permission semantics.</action>
<acceptance_criteria>UoW source contains the three exact GUC names and no `sql.raw`; context fields are non-optional; member service has no root `database.db` domain query and still contains explicit churchId predicate/value; pre-task member diff remains present; targeted tests exit 0.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/database/tenant-unit-of-work.spec.ts apps/api/src/tenancy</automated></verify>
<done>Member operations are minimally adapted to the explicit transaction boundary without becoming a completed member feature.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Classify every route and deny missing domain permission</name>
<files>apps/api/src/tenancy/route-policy.decorator.ts, apps/api/src/tenancy/route-policy.guard.ts, apps/api/src/tenancy/domain-route.decorator.ts, apps/api/src/tenancy/route-policy-inventory.spec.ts, apps/api/src/tenancy/permissions.guard.ts, apps/api/src/tenancy/permissions.guard.spec.ts, apps/api/src/tenancy/tenancy.module.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/churches/churches.controller.ts, apps/api/src/members/members.controller.ts</files>
<read_first>apps/api/src/auth/auth.controller.ts, apps/api/src/churches/churches.controller.ts, apps/api/src/members/members.controller.ts, apps/api/src/tenancy/permissions.guard.ts, apps/api/src/tenancy/permissions.guard.spec.ts, apps/api/src/tenancy/tenancy.module.ts, packages/authorization/src/policy.ts</read_first>
<behavior>- Unclassified route is 403. - Domain route with empty/missing/unknown permission is denied. - DomainRoute applies SessionAuthGuard, TenantGuard, PermissionsGuard in that order.</behavior>
<action>Create exactly four classifications: public, identity, internal, domain. Register RoutePolicyGuard as APP_GUARD from TenancyModule. Add `DomainRoute(permission,...permissions)` with a required first `ChurchPermission`; annotate church/member handlers through it, public login/health explicitly, identity logout/me explicitly, and later internal metrics through InternalRoute. Change PermissionsGuard missing/empty metadata from allow to ForbiddenException. Inventory registered controllers with DiscoveryService and assert one classification per handler and non-empty known permissions for domain routes. Preserve the dirty members permissions and centralized matrix; do not add new member capabilities.</action>
<acceptance_criteria>`permissions.guard.ts` has no `return true` for missing metadata; DomainRoute contains guards in exact auth→tenant→permission order; inventory test enumerates controllers and rejects missing/multiple classifications; all current controller methods carry a classification; targeted command exits 0.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/tenancy/route-policy-inventory.spec.ts apps/api/src/tenancy/permissions.guard.spec.ts packages/authorization/src/policy.spec.ts</automated></verify>
<done>All routes fail closed and domain routes expose their least-privilege capability declaratively.</done>
</task>
</tasks>
<verification>`pnpm exec vitest run apps/api/src/database apps/api/src/tenancy packages/authorization/src/policy.spec.ts` passes.</verification>
<success_criteria>Missing tenant or permission fails before domain access, platform administrators still select a church, and members work is only foundation-adapted.</success_criteria>
<output>After completion, create `.planning/phases/01-tenant-and-production-safety-foundation/01-02-SUMMARY.md`.</output>
