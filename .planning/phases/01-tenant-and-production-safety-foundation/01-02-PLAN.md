---
phase: 01-tenant-and-production-safety-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - apps/api/src/database/tenant-unit-of-work.ts
  - apps/api/src/database/tenant-unit-of-work.spec.ts
  - apps/api/src/database/database.module.ts
  - apps/api/src/auth/auth.types.ts
  - apps/api/src/members/members.service.ts
  - apps/api/src/members/members.controller.ts
autonomous: true
requirements: [TEN-01, TEN-02]
must_haves:
  truths:
    - "Domain work requires non-optional church, actor, and correlation context."
    - "Tenant GUC state is local to the exact transaction and member queries retain explicit predicates."
  artifacts:
    - path: "apps/api/src/database/tenant-unit-of-work.ts"
      provides: "Typed transaction-local tenant boundary"
  key_links:
    - from: "apps/api/src/members/members.service.ts"
      to: "apps/api/src/database/tenant-unit-of-work.ts"
      via: "run(context, callback transaction)"
      pattern: "tenantUnitOfWork.*run"
---
<objective>Introduce the explicit tenant unit of work and minimally adapt the dirty member slice. Purpose: implement D-01/D-02/D-04 without completing Phase 3. Output: typed TenantContext and transaction-only member access.</objective>
<execution_context>@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-01-SUMMARY.md
@AGENTS.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Define and test explicit tenant transactions</name>
<files>apps/api/src/database/tenant-unit-of-work.ts, apps/api/src/database/tenant-unit-of-work.spec.ts, apps/api/src/database/database.module.ts, apps/api/src/auth/auth.types.ts</files>
<read_first>apps/api/src/database/database.service.ts, apps/api/src/database/database.module.ts, apps/api/src/auth/auth.types.ts, packages/database/src/index.ts, packages/database/scripts/test-tenant-isolation.ts, 01-01-SUMMARY.md preservation manifest location</read_first>
<behavior>- readonly churchId/actorId/correlationId are required. - one Drizzle callback transaction sets all three GUCs with local=true. - missing context cannot call work.</behavior>
<action>Verify protected-file SHA-256 values from Plan 01 before editing. Define immutable TenantContext and TenantTransaction. Implement `run&lt;T&gt;(context, work)` using one Drizzle transaction and parameterized `set_config` for `app.current_church_id`, `app.current_actor_id`, `app.correlation_id`, each `true`; never use sql.raw, pool.query BEGIN, optional IDs, ambient storage, or session SET. Export the provider from DatabaseModule.</action>
<acceptance_criteria>Context has three readonly non-optional string fields; source contains all three exact GUCs and three local `true` values; tests assert callback receives the same transaction and is not invoked when context validation fails; non-owned protected hashes match.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/database/tenant-unit-of-work.spec.ts</automated></verify>
<done>The safe transaction boundary is independently tested and injectable.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Move only existing member operations behind the unit of work</name>
<files>apps/api/src/members/members.service.ts, apps/api/src/members/members.controller.ts</files>
<read_first>apps/api/src/members/members.service.ts, apps/api/src/members/members.controller.ts, apps/api/src/auth/auth.types.ts, apps/api/src/database/tenant-unit-of-work.ts, protected backups/manifest from Plan 01</read_first>
<behavior>- list/create use TenantContext and callback tx. - list still predicates church_id. - create supplies context.churchId.</behavior>
<action>Compare both dirty files to their SHA-256-backed Plan 01 copies. Adapt only signatures/injection needed for TenantUnitOfWork; preserve every pre-existing member contract, validation, permission, response, and error line. Keep explicit `eq(members.churchId, context.churchId)` and inserted churchId inside the callback. Do not add search, pagination, lifecycle, duplicate detection, UI, envelope/PDF storage, or other member scope.</action>
<acceptance_criteria>No root `database.db` remains in MembersService; explicit church predicate and insert value remain; backup comparison shows only UoW/context adaptations; every other protected file hash is unchanged; targeted tests pass.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/database apps/api/src/tenancy</automated></verify>
<done>Existing member operations use the safe boundary without becoming feature completion.</done>
</task>
</tasks>
<verification>`pnpm exec vitest run apps/api/src/database apps/api/src/tenancy &amp;&amp; pnpm --filter @uckg/database test:tenancy` passes.</verification>
<success_criteria>Missing tenant context cannot reach domain SQL and pooled context cannot escape a transaction.</success_criteria>
<output>After completion create `01-02-SUMMARY.md`.</output>
