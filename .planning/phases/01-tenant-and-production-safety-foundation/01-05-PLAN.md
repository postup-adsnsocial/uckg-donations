---
phase: 01-tenant-and-production-safety-foundation
plan: 05
type: execute
wave: 4
depends_on: [01-03, 01-04]
files_modified:
  - apps/api/src/security/login-attempt-store.ts
  - apps/api/src/security/login-attempt-store.spec.ts
  - apps/api/src/security/login-source-throttler.guard.ts
  - apps/api/src/security/login-source-throttler.guard.spec.ts
  - apps/api/src/auth/auth.service.ts
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/auth.module.ts
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "Every login attempt is source-throttled and only failed authentication increments the account window."
    - "Unknown, inactive, wrong-password, source-limited, and account-limited outcomes do not enumerate accounts."
  artifacts:
    - path: "apps/api/src/security/login-attempt-store.ts"
      provides: "Exact expiring failure counter flow"
  key_links:
    - from: "apps/api/src/auth/auth.controller.ts"
      to: "apps/api/src/security/login-attempt-store.ts"
      via: "precheck then failure increment or success reset"
      pattern: "check.*recordFailure|reset"
---
<objective>Implement precise non-enumerating login throttling. Purpose: implement D-10/D-12 with deterministic failure-only account counters. Output: source guard, expiring account store, dummy verification and generic responses.</objective>
<execution_context>@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-03-SUMMARY.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-04-SUMMARY.md
@AGENTS.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Implement source guard and expiring failed-account store</name>
<files>apps/api/src/security/login-attempt-store.ts, apps/api/src/security/login-attempt-store.spec.ts, apps/api/src/security/login-source-throttler.guard.ts, apps/api/src/security/login-source-throttler.guard.spec.ts</files>
<read_first>apps/api/src/config/api-config.service.ts, apps/api/src/auth/auth.controller.ts, packages/contracts/src/index.ts, @nestjs/throttler installed API sources</read_first>
<behavior>- source: all attempts, 10/60s. - account: failed attempts only, 5/15m. - expired entry is deleted; success reset deletes entry. - keys are SHA-256 normalized emails.</behavior>
<action>Use @nestjs/throttler for source tracking from request.ip after validated proxy. Implement injectable in-memory account failure store with methods `check(key, now)`, `recordFailure(key, now)`, `reset(key)`; entry is `{count, expiresAt}`; check/record first delete when `expiresAt &lt;= now`; first failure sets now+900000; fifth failure makes subsequent precheck generic 429 until expiry; success calls reset. Hash trimmed lowercase email with SHA-256 before storage. Bound cleanup by deleting expired entries during operations. Document single-replica limitation and need for edge/shared storage before replicas; do not add Redis/Postgres adapter.</action>
<acceptance_criteria>Tests use fake time and prove all-attempt source flow, 1..5 failures, sixth precheck rejection, expiry deletion, success reset, independent accounts/origins, proxy semantics, and no raw email key; commands pass.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/security/login-attempt-store.spec.ts apps/api/src/security/login-source-throttler.guard.spec.ts</automated></verify>
<done>Counter semantics and TTL are executable and deterministic.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Wire generic authentication outcomes and dummy work</name>
<files>apps/api/src/auth/auth.service.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.module.ts</files>
<read_first>apps/api/src/auth/auth.service.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.module.ts, apps/api/src/security/login-attempt-store.ts, apps/api/src/security/login-source-throttler.guard.ts, packages/authorization/src/password.ts, protected backups from Plan 01</read_first>
<behavior>- precheck occurs before auth. - failed auth records exactly once. - successful auth resets. - malformed body is source-limited but never account-counted. - cookie remains HttpOnly/Strict/Secure production.</behavior>
<action>Because Plan 03 exclusively classified auth.controller and this plan depends on it, preserve its route decorators while adding the source guard and account sequence. Normalize once; precheck account hash; authenticate; on Unauthorized record one failure then return identical generic 401; on success reset then issue session. Unknown/inactive user performs one fixed valid dummy scrypt verification. Map source/account limits to identical 429 body. Keep password 6–128 and cookie flags. No logs contain identifier/hash.</action>
<acceptance_criteria>Tests spy that failure increments once, success resets, malformed body does not account-increment, unknown/inactive/wrong share status/body and dummy verification, both limits share 429, cookie flags remain; protected auth backup differs only planned decorator/security changes.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/security apps/api/src/auth packages/authorization/src/password.spec.ts</automated></verify>
<done>Login behavior is bounded, generic, and preserves locked password/cookie decisions.</done>
</task>
</tasks>
<verification>`pnpm exec vitest run apps/api/src/security apps/api/src/auth` passes.</verification>
<success_criteria>Source and failure-only account abuse limits have precise expiry/reset behavior without enumeration.</success_criteria>
<output>After completion create `01-05-SUMMARY.md`.</output>
