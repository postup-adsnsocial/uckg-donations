---
phase: 01-tenant-and-production-safety-foundation
plan: 07
type: execute
wave: 5
depends_on: [01-02, 01-05, 01-06]
files_modified:
  - tests/e2e/production-safety.spec.ts
  - playwright.production-safety.config.ts
  - scripts/run-production-safety-e2e.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEN-01, TEN-02, TEN-03, SEC-01, SEC-02, SEC-03]
must_haves:
  truths:
    - "The production-mode API runs as the real runtime role while migrations run separately."
    - "A controlled database outage leaves live up, makes ready generic-fail, and recovers cleanly."
    - "The complete phase gate proves headers, cookies, throttles, errors, tenant isolation, health, metrics and cleanup."
  artifacts:
    - path: "scripts/run-production-safety-e2e.ts"
      provides: "Deterministic production launch/outage/recovery fixture"
    - path: "tests/e2e/production-safety.spec.ts"
      provides: "Adversarial HTTP acceptance suite"
  key_links:
    - from: "scripts/run-production-safety-e2e.ts"
      to: "playwright.production-safety.config.ts"
      via: "migrate/provision/start/test/cleanup lifecycle"
      pattern: "MIGRATION_DATABASE_URL|DATABASE_URL|NODE_ENV"
---
<objective>Prove Phase 1 through a controlled production-mode runtime fixture. Purpose: close D-17 through D-19 and all six requirements. Output: owned launch harness, adversarial E2E, CI/full gate.</objective>
<execution_context>@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-05-SUMMARY.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-06-SUMMARY.md
@AGENTS.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Create exact production launch and database outage fixture</name>
<files>playwright.production-safety.config.ts, scripts/run-production-safety-e2e.ts, package.json, .github/workflows/ci.yml</files>
<read_first>playwright.config.ts, package.json, .github/workflows/ci.yml, docker-compose.yml, packages/database/scripts/migrate.ts, packages/database/scripts/postgres-test-harness.ts, apps/api/src/config/api-config.ts</read_first>
<behavior>- migrator applies schema with MIGRATION_DATABASE_URL then exits. - API starts NODE_ENV=production with runtime DATABASE_URL and all required safe env. - outage is controlled and recovered; processes/roles/databases always cleaned.</behavior>
<action>Create root `test:production-safety` runner. It creates an isolated DB plus generated LOGIN runtime member, runs `pnpm db:migrate` with only MIGRATION_DATABASE_URL, then starts built API with NODE_ENV=production, runtime DATABASE_URL, API_PORT=3101, WEB_ORIGINS=http://127.0.0.1:3100, explicit TRUST_PROXY=false, METRICS_TOKEN=32+ char canary-safe token and every required limit; assert current_user is runtime login. Launch Playwright against 3101. For outage, terminate/deny connections to the isolated database (not the shared Docker service), assert live/ready behavior, restore connectivity, wait until ready 200. In finally stop API, terminate connections, drop runtime login/database, and restore no global state. CI owns both URLs and runs this script. Never expose migrator URL to API.</action>
<acceptance_criteria>Runner source contains explicit production env values, separate URLs, readiness wait, outage, recovery and finally cleanup; API child env deletes MIGRATION_DATABASE_URL; CI invokes test:production-safety; config files are all listed in this plan.</acceptance_criteria>
<verify><automated>pnpm --filter @uckg/api build &amp;&amp; pnpm test:production-safety -- --list</automated></verify>
<done>A reproducible real-role production fixture owns launch through cleanup.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Cover every production-safety behavior and run final gates</name>
<files>tests/e2e/production-safety.spec.ts</files>
<read_first>tests/e2e/auth-tenancy.spec.ts, playwright.production-safety.config.ts, scripts/run-production-safety-e2e.ts, AGENTS.md, .planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md</read_first>
<behavior>- Helmet/CORS/413/source+account 429/cookie Secure+HttpOnly+Strict/generic errors/correlation. - missing/cross tenant and permission denial. - live/ready outage/recovery and metrics token/content/redaction.</behavior>
<action>Write serial APIRequestContext tests against the production fixture. Seed fixtures through migrator/admin setup before runtime start, never through privileged connection during assertions. Verify the complete behavior list, including identical auth outcomes and canary absence. No browser page/rendered UI is changed; therefore no new visual baseline. Run database tenancy, E2E and full gate; if any rendered output unexpectedly changes, enforce PT-BR/en/es desktop/mobile/narrow and inspect diffs before baseline update. Do not add envelope image/PDF storage.</action>
<acceptance_criteria>Spec contains assertions for Secure/HttpOnly/SameSite=Strict, Helmet, allow/reject CORS, 413, both 429 scopes, correlation, generic error, missing/cross tenant, permission, live 200 during outage, ready 503 then 200 recovery, metrics 401/200/content type/no canaries; all commands exit 0.</acceptance_criteria>
<verify><automated>pnpm test:production-safety</automated></verify>
<done>All six Phase 1 requirements pass under the actual production-mode runtime role.</done>
</task>
</tasks>
<verification>`pnpm test &amp;&amp; pnpm test:migrations &amp;&amp; pnpm --filter @uckg/database test:tenancy &amp;&amp; pnpm test:production-safety &amp;&amp; pnpm check:full` passes.</verification>
<success_criteria>Production startup, runtime role, outage/recovery and security/observability behaviors are reproducible and cleanly torn down.</success_criteria>
<output>After completion create `01-07-SUMMARY.md`.</output>
