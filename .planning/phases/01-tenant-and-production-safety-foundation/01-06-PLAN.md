---
phase: 01-tenant-and-production-safety-foundation
plan: 06
type: execute
wave: 4
depends_on: [01-03, 01-04]
files_modified:
  - apps/api/src/observability/logger.config.ts
  - apps/api/src/observability/logger.config.spec.ts
  - apps/api/src/observability/safe-exception.filter.ts
  - apps/api/src/observability/metrics.service.ts
  - apps/api/src/observability/metrics.service.spec.ts
  - apps/api/src/observability/metrics.interceptor.ts
  - apps/api/src/observability/observability.controller.ts
  - apps/api/src/observability/observability.module.ts
  - apps/api/src/health/health.controller.ts
  - apps/api/src/health/health.controller.spec.ts
  - apps/api/src/app.module.ts
  - apps/api/src/main.ts
autonomous: true
requirements: [SEC-02, SEC-03]
must_haves:
  truths:
    - "Logs/errors correlate requests without secrets, PII, tenant identifiers, SQL, or config values."
    - "Public liveness is dependency-free; public readiness is generic; internal metrics require a token."
  artifacts:
    - path: "apps/api/src/observability/logger.config.ts"
      provides: "JSON allowlist logging and correlation"
    - path: "apps/api/src/observability/metrics.service.ts"
      provides: "Bounded Prometheus registry"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "apps/api/src/observability/logger.config.ts"
      via: "bufferLogs and useLogger"
      pattern: "bufferLogs|useLogger"
---
<objective>Add safe correlation, errors, health, and metrics. Purpose: implement D-14 through D-16 while retaining explicit health classification from Plan 03. Output: Pino configuration, exception filter, live/ready, protected low-cardinality metrics.</objective>
<execution_context>@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-03-SUMMARY.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-04-SUMMARY.md
@AGENTS.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Build correlation, allowlist JSON logging, and safe errors</name>
<files>apps/api/src/observability/logger.config.ts, apps/api/src/observability/logger.config.spec.ts, apps/api/src/observability/safe-exception.filter.ts, apps/api/src/observability/observability.module.ts</files>
<read_first>apps/api/src/main.ts, apps/api/src/config/api-config.service.ts, apps/api/src/auth/auth.controller.ts, .planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md</read_first>
<behavior>- valid UUID correlation retained; invalid replaced. - JSON allowlist excludes all canaries. - public errors contain status/message/correlationId only.</behavior>
<action>Configure nestjs-pino genReqId and x-correlation-id. Allow only correlation, method, route template, status class, duration, bounded event/outcome/scope. Redact authorization, cookie, x-metrics-token, body, set-cookie, password, token, email, fullName, phone. SafeExceptionFilter never emits/logs stack, SQL, config, raw errors. Canary tests inject password, session token, auth header, cookie, email, name, phone, church UUID and DB URL byte-for-byte.</action>
<acceptance_criteria>Every captured line parses JSON with UUID; all canaries absent; 4xx/5xx body has correlationId and no internals; tests pass.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/observability/logger.config.spec.ts</automated></verify>
<done>Operational logs and errors are safely correlated.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Implement classified live/ready and protected bounded metrics</name>
<files>apps/api/src/observability/metrics.service.ts, apps/api/src/observability/metrics.service.spec.ts, apps/api/src/observability/metrics.interceptor.ts, apps/api/src/observability/observability.controller.ts, apps/api/src/health/health.controller.ts, apps/api/src/health/health.controller.spec.ts</files>
<read_first>apps/api/src/health/health.controller.ts, apps/api/src/health/health.controller.spec.ts, apps/api/src/tenancy/route-policy.decorator.ts, apps/api/src/database/database.service.ts, apps/api/src/config/api-config.service.ts</read_first>
<behavior>- /health and /health/live public and DB-independent. - /health/ready public, SELECT 1 bounded at 1s, generic 503. - /internal/metrics internal, constant-time token, fixed labels.</behavior>
<action>Retain/add PublicRoute on all health handlers and InternalRoute on metrics. Dedicated registry exposes `uckg_api_http_requests_total{method,route,status_class}`, duration with same labels, login attempts outcome, throttle rejections scope, pool connections state total/idle/waiting, and safe defaults. Metrics token comparison is length-safe/timingSafeEqual and response uses registry content type. Never label raw URL, tenant/user/correlation/email/error.</action>
<acceptance_criteria>Health tests simulate DB failure with live 200 and ready generic 503; metrics tests assert exact names/labels, absent/wrong token denial, correct content type, no canaries; route inventory remains green.</acceptance_criteria>
<verify><automated>pnpm exec vitest run apps/api/src/health apps/api/src/observability apps/api/src/tenancy/route-policy-inventory.spec.ts</automated></verify>
<done>Health and metrics are useful, explicitly classified, and non-sensitive.</done>
</task>
<task type="auto">
<name>Task 3: Wire observability into the validated bootstrap</name>
<files>apps/api/src/app.module.ts, apps/api/src/main.ts</files>
<read_first>apps/api/src/app.module.ts, apps/api/src/main.ts, apps/api/src/observability/observability.module.ts, apps/api/src/observability/safe-exception.filter.ts, 01-04-SUMMARY.md</read_first>
<action>Starting from Plan 04 output, register LoggerModule/ObservabilityModule, global metrics interceptor and safe filter; create Nest with bufferLogs true then app.useLogger. Preserve every validated Helmet/CORS/proxy/body/pool setting. Do not alter auth.controller (owned by Plan 05 in the same wave).</action>
<acceptance_criteria>Main contains bufferLogs/useLogger and retains helmet, CORS, proxy and 256kb; AppModule wires observability/filter/interceptor; typecheck and focused tests pass.</acceptance_criteria>
<verify><automated>pnpm --filter @uckg/api typecheck &amp;&amp; pnpm exec vitest run apps/api/src/observability apps/api/src/health</automated></verify>
<done>Safe observability is active without losing bootstrap protections.</done>
</task>
</tasks>
<verification>`pnpm exec vitest run apps/api/src/observability apps/api/src/health apps/api/src/tenancy` passes.</verification>
<success_criteria>Responsible operators can distinguish process, dependency, traffic, error, throttle and pool state without sensitive data.</success_criteria>
<output>After completion create `01-06-SUMMARY.md`.</output>
