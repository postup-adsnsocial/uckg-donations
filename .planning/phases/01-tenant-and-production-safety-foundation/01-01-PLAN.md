---
phase: 01-tenant-and-production-safety-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/schema.ts
  - packages/database/migrations/0002_tenant_runtime_rls.sql
  - packages/database/migrations/meta/0002_snapshot.json
  - packages/database/migrations/meta/_journal.json
  - packages/database/scripts/postgres-test-harness.ts
  - packages/database/scripts/test-migrations.ts
  - packages/database/scripts/migrate.ts
  - packages/database/scripts/test-tenant-isolation.ts
  - packages/database/package.json
  - package.json
  - docker-compose.yml
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEN-01, TEN-02]
must_haves:
  truths:
    - "A runtime database login sees no member rows and cannot insert when tenant context is absent."
    - "Church A data cannot be read, changed, or associated while church B is active, including after pooled connection reuse."
    - "Runtime and migration credentials are separate and the runtime login cannot bypass RLS or perform schema/destructive operations."
  artifacts:
    - path: "packages/database/migrations/0002_tenant_runtime_rls.sql"
      provides: "Composite member key, least-privilege role grants, and forced RLS policy"
    - path: "packages/database/scripts/test-tenant-isolation.ts"
      provides: "Real-role PostgreSQL adversarial proof"
    - path: "packages/database/scripts/postgres-test-harness.ts"
      provides: "Reusable isolated database and login-role fixture"
  key_links:
    - from: "packages/database/scripts/test-tenant-isolation.ts"
      to: "packages/database/migrations/0002_tenant_runtime_rls.sql"
      via: "actual LOGIN member of uckg_runtime with Pool max 1"
      pattern: "row_security_active|rolbypassrls|current_user"
    - from: "packages/database/src/schema.ts"
      to: "packages/database/migrations/0002_tenant_runtime_rls.sql"
      via: "members (church_id,id) candidate key"
      pattern: "members_church_id_id_unique"
---

<objective>
Make PostgreSQL independently enforce tenant isolation under the credential used by the API.

Purpose: Satisfy D-03/D-04/D-17 with real PostgreSQL proof while preserving—not completing—the dirty members slice.
Output: Additive RLS migration, split-role test/provisioning path, and adversarial tenancy suite.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the isolated PostgreSQL and runtime-role test harness</name>
  <files>packages/database/scripts/postgres-test-harness.ts, packages/database/scripts/test-migrations.ts, packages/database/scripts/test-tenant-isolation.ts, packages/database/package.json, package.json</files>
  <read_first>packages/database/scripts/test-migrations.ts, packages/database/scripts/migrate.ts, packages/database/package.json, package.json, packages/database/src/schema.ts, packages/database/migrations/meta/_journal.json, packages/database/migrations/0001_confused_dragon_man.sql, packages/database/migrations/meta/0001_snapshot.json, apps/api/src/app.module.ts, apps/api/src/members/members.controller.ts, apps/api/src/members/members.module.ts, apps/api/src/members/members.service.ts, packages/authorization/src/policy.ts, packages/authorization/src/policy.spec.ts, packages/contracts/src/index.ts, .planning/phases/01-tenant-and-production-safety-foundation/01-VALIDATION.md</read_first>
  <behavior>- Migration test retains its existing five-table members assertion. - Tenancy test connects through a freshly created LOGIN role granted to uckg_runtime, never SET ROLE. - Cleanup terminates clients and drops temporary roles/database in finally blocks.</behavior>
  <action>Before any edit, create a temporary backup directory and SHA-256 manifest for every protected tracked or untracked file: `apps/api/src/app.module.ts`, every file from `find apps/api/src/members -type f`, `packages/authorization/src/policy.ts`, `policy.spec.ts`, `packages/contracts/src/index.ts`, `packages/database/src/schema.ts`, `packages/database/scripts/test-migrations.ts`, `packages/database/migrations/0001_confused_dragon_man.sql`, `packages/database/migrations/meta/0001_snapshot.json`, and `_journal.json`; copy each file to the backup. Record which owned files are expected to change and require all other hashes to remain identical after every task. For owned dirty files, compare against the backup and allow only the additive instructions in this plan—never regenerate/replace 0001. Extract database creation/migration/cleanup into `postgres-test-harness.ts` without deleting the members assertion. Change `migrate.ts` to require non-empty `MIGRATION_DATABASE_URL` and throw naming that key when absent; it must never read DATABASE_URL or use a local fallback. Add `test:tenancy` scripts and scaffold a real LOGIN-role test that intentionally exits nonzero before Task 2 supplies RLS.</action>
  <acceptance_criteria>The SHA-256 manifest includes every path returned by the explicit list and `find apps/api/src/members -type f`; hashes for non-owned protected files match; `rg 'MIGRATION_DATABASE_URL' packages/database/scripts/migrate.ts` matches and `rg 'process.env.DATABASE_URL|localhost' packages/database/scripts/migrate.ts` does not; the migration test retains members; the new tenancy command executes and is expected-red at this point.</acceptance_criteria>
  <verify><automated>pnpm test:migrations &amp;&amp; (pnpm --filter @uckg/database test:tenancy; test $? -ne 0)</automated></verify>
  <done>The reusable harness is green for migrations and the owned tenancy test scaffold exists without loss of dirty member work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add composite tenant key, least-privilege grants, and forced RLS</name>
  <files>packages/database/src/schema.ts, packages/database/migrations/0002_tenant_runtime_rls.sql, packages/database/migrations/meta/0002_snapshot.json, packages/database/migrations/meta/_journal.json, packages/database/scripts/test-migrations.ts</files>
  <read_first>packages/database/src/schema.ts, packages/database/migrations/0001_confused_dragon_man.sql, packages/database/migrations/meta/0001_snapshot.json, packages/database/migrations/meta/_journal.json, packages/database/scripts/test-migrations.ts, packages/database/scripts/test-tenant-isolation.ts, the temporary SHA-256 manifest and backups created by Task 1</read_first>
  <behavior>- Missing/empty app.current_church_id yields no rows, not a UUID cast error. - Context B cannot read/write A. - Runtime lacks owner, superuser, BYPASSRLS, CREATE, TRUNCATE, REFERENCES and migration-table access.</behavior>
  <action>Add Drizzle unique constraint `members_church_id_id_unique` on `(churchId,id)` only; preserve all current member columns/indexes/checks. Run `pnpm db:generate --name tenant_runtime_rls` so the additive artifacts are exactly 0002, then append SQL creating idempotent NOLOGIN `uckg_runtime` with NOSUPERUSER/NOCREATEDB/NOCREATEROLE/NOREPLICATION/NOBYPASSRLS; revoke PUBLIC schema CREATE; grant schema USAGE, control-plane minimum privileges, admin_sessions SELECT/INSERT/UPDATE/DELETE, and members SELECT/INSERT/UPDATE only. Enable and FORCE RLS on members. Create `members_tenant_isolation` FOR ALL TO uckg_runtime with identical USING/WITH CHECK expression `church_id = NULLIF(current_setting('app.current_church_id', true), '')::uuid`. Extend migration catalog checks for relrowsecurity, relforcerowsecurity, policy roles/expressions, composite unique, ownership, and grants. Do not grant DELETE/TRUNCATE/REFERENCES on members.</action>
  <acceptance_criteria>0002 contains `FORCE ROW LEVEL SECURITY`, `TO uckg_runtime`, both `USING` and `WITH CHECK`, and `NULLIF(current_setting('app.current_church_id', true), '')::uuid`; schema and SQL contain `members_church_id_id_unique`; SHA-256 for 0001 SQL/snapshot and all non-owned protected files equals the manifest; backup comparison proves owned schema/test/journal retain all baseline member content plus only planned additions; `pnpm test:migrations` exits 0.</acceptance_criteria>
  <verify><automated>pnpm test:migrations</automated></verify>
  <done>Migration chain proves the candidate key, policy, forced RLS, ownership, and least-privilege catalog state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove isolation through the real runtime login and CI topology</name>
  <files>packages/database/scripts/test-tenant-isolation.ts, docker-compose.yml, .github/workflows/ci.yml</files>
  <read_first>packages/database/scripts/test-tenant-isolation.ts, packages/database/scripts/postgres-test-harness.ts, packages/database/migrations/0002_tenant_runtime_rls.sql, docker-compose.yml, .github/workflows/ci.yml</read_first>
  <behavior>- A/B/no-context/A repeats 25 times on Pool max 1 without leakage. - Cross-tenant composite child FK fails. - row_security=off and prohibited runtime operations error.</behavior>
  <action>Complete the test with parameterized transaction-local `set_config` for church, actor, and correlation; assert current_user login attributes, table non-ownership, row_security_active, no-context read/write denial, A success, B raw-query isolation, B writing church A denial, test-only composite child rejection, 25 A/B/none/A reuse cycles including rollback, `row_security=off` failure, and denial of TRUNCATE/schema CREATE/policy alteration/migration journal. Configure local/CI variables `MIGRATION_DATABASE_URL` and `DATABASE_URL` separately; migrations use only the former and API/E2E use a generated runtime LOGIN URL. Do not introduce provider-specific Supabase code or hardcode us-east-1.</action>
  <acceptance_criteria>Test source contains `max: 1`, a loop bound of at least 25, `row_security = off`, `TRUNCATE`, and composite FK rejection; CI contains both `MIGRATION_DATABASE_URL` and runtime `DATABASE_URL`, runs migrations before `test:tenancy`, and never runs the API with the migrator URL; command exits 0.</acceptance_criteria>
  <verify><automated>pnpm --filter @uckg/database test:tenancy &amp;&amp; pnpm test:migrations</automated></verify>
  <done>Real-role attacks demonstrate TEN-01/TEN-02 independently of API predicates.</done>
</task>

</tasks>

<verification>`pnpm test &amp;&amp; pnpm test:migrations &amp;&amp; pnpm --filter @uckg/database test:tenancy` passes.</verification>
<success_criteria>Runtime-role PostgreSQL denies missing/cross-tenant access and unsafe privileges while the dirty members slice remains intact and incomplete.</success_criteria>
<output>After completion, create `.planning/phases/01-tenant-and-production-safety-foundation/01-01-SUMMARY.md`.</output>
