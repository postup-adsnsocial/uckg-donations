---
phase: quick
plan: 260804-0av
type: execute
autonomous: true
files_modified:
  - packages/contracts/src/index.ts
  - apps/api/src/churches/churches.controller.ts
  - apps/api/src/churches/churches.service.ts
  - apps/api/src/churches/churches.module.ts
  - apps/api/src/churches/churches.service.spec.ts
  - apps/web/app/components/app-shell.tsx
  - apps/web/app/churches/churches-page.tsx
  - apps/web/app/[locale]/churches/page.tsx
  - apps/web/app/members/member-form-page.tsx
  - apps/web/app/i18n/dictionaries.ts
  - apps/web/app/i18n/product-copy.ts
  - apps/web/app/styles.css
  - tests/e2e/auth-tenancy.spec.ts
  - tests/e2e/mvp-browser-check.mjs
---

<objective>
Add minimal church registration for platform administrators and allow them to choose the owning church while creating a member, while keeping an existing member's church immutable.

Purpose: Let platform administration bootstrap congregations without weakening tenant boundaries.
Output: Protected church list/create API, localized church-management UI, tenant-aware member creation, and regression coverage.
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@apps/api/src/churches/churches.controller.ts
@apps/api/src/tenancy/tenant.guard.ts
@apps/api/src/tenancy/tenant.service.ts
@apps/web/app/components/app-shell.tsx
@apps/web/app/members/member-form-page.tsx
@packages/contracts/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add platform-admin-only church list and creation API</name>
  <files>packages/contracts/src/index.ts, apps/api/src/churches/churches.controller.ts, apps/api/src/churches/churches.service.ts, apps/api/src/churches/churches.module.ts, apps/api/src/churches/churches.service.spec.ts</files>
  <behavior>
    - An authenticated platform administrator can list churches and create a church from a validated minimal payload (name and slug, with existing schema defaults for locale/timezone/status).
    - A non-platform administrator receives 403 from both global church-management operations.
    - Duplicate slugs return 409; invalid payloads return 400.
  </behavior>
  <action>Create shared Zod request/response contracts, then add a ChurchesService backed by DatabaseService and identity-scoped GET/POST `/churches` handlers. Protect these handlers with SessionAuthGuard plus an explicit `isPlatformAdmin` authorization check; do not put global church management behind TenantGuard or accept `x-church-id` as authority. Preserve the existing tenant-scoped `/churches/current` and `/churches/current/settings` routes unchanged. Return only fields needed by selectors/listing and order churches by name.</action>
  <verify>
    <automated>pnpm vitest run apps/api/src/churches/churches.service.spec.ts packages/authorization/src/policy.spec.ts</automated>
  </verify>
  <done>Platform admins can create/list churches, ordinary tenant roles are denied, validation and duplicate handling are covered, and existing current-church tenant resolution still works.</done>
</task>

<task type="auto">
  <name>Task 2: Build church management and tenant-aware member creation UI</name>
  <files>apps/web/app/components/app-shell.tsx, apps/web/app/churches/churches-page.tsx, apps/web/app/[locale]/churches/page.tsx, apps/web/app/members/member-form-page.tsx, apps/web/app/i18n/dictionaries.ts, apps/web/app/i18n/product-copy.ts, apps/web/app/styles.css</files>
  <action>Add a platform-admin-only Churches navigation item and a responsive page that lists churches and submits the minimal create form. For platform admins, load active churches from GET `/churches` into AppShell's church selector even when they have no membership in those churches; retain membership-derived choices for every other role. In the new-member form only, show a church selector to platform admins, send POST `/members` with the chosen church in `x-church-id`, and persist that selection before navigating to the saved member so its detail request uses the same tenant. For edit/existing-member flows, render the resolved church as read-only and never allow reassignment. Add every new label, validation message, empty state, and accessible name in PT-BR, English, and Spanish. Style controls for desktop/mobile/narrow layouts with no horizontal overflow and at least 44px interactive targets.</action>
  <verify>
    <automated>pnpm --filter @uckg/web typecheck &amp;&amp; pnpm lint</automated>
  </verify>
  <done>A platform admin can register a church and choose it when creating a member; non-platform users see only their authorized church; existing member church ownership cannot be edited; all three locales remain accessible and responsive.</done>
</task>

<task type="auto">
  <name>Task 3: Prove authorization, tenant ownership, and responsive flows</name>
  <files>tests/e2e/auth-tenancy.spec.ts, tests/e2e/mvp-browser-check.mjs</files>
  <action>Extend API E2E coverage to prove platform-admin allow/non-platform deny behavior and that creating a member under church B cannot be read through church A. Extend the MVP browser flow with a platform-admin fixture to create/list a church, select it on new-member creation, and confirm the saved member displays that church read-only. Exercise PT-BR, English, and Spanish at desktop, mobile, and 320px narrow widths; assert no horizontal overflow and capture screenshots for inspection. Run functional checks first, then `pnpm test:e2e`, and `pnpm test:visual` last. Inspect any actual/diff images before accepting or intentionally updating snapshots.</action>
  <verify>
    <automated>pnpm test:mvp &amp;&amp; pnpm check:full</automated>
  </verify>
  <done>Automated tests prove authorization and cross-tenant denial, the complete browser flow passes in all locales and widths, reviewed screenshots have no clipping/overflow/sub-44px primary targets, and the final full quality gate passes.</done>
</task>

</tasks>

<verification>
Run `pnpm test:mvp` for the feature flow, then `pnpm check:full` as the final gate. Confirm the existing member detail/edit screens never expose a church reassignment control and all member API calls remain explicitly scoped by `x-church-id`.
</verification>

<success_criteria>
- Only platform administrators can list or create churches globally.
- Platform administrators can select an active church while creating a member, and the member is stored/read only in that tenant.
- Existing members show their church read-only and cannot be moved between tenants.
- PT-BR, English, and Spanish layouts pass desktop, mobile, and narrow visual review without overflow or inaccessible targets.
</success_criteria>

<output>
After completion, create `.planning/quick/260804-0av-criar-cadastro-m-nimo-de-igrejas-para-ad/260804-0av-SUMMARY.md`.
</output>
